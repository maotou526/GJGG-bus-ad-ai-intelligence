---
globs: *.py
description: Python Django开发规范和编码标准
---

# Python Django 开发规范

## 代码风格和规范

### 基本编码规范
- 使用4个空格进行缩进，不使用Tab
- 行长度不超过120个字符
- 函数和方法之间使用两个空行分隔
- 类和顶级函数定义之间使用两个空行分隔
- 导入语句按照以下顺序分组：标准库、第三方库、本地应用
- 括号()里面的参数、条件等内容不要换行

### 命名规范
- **类名**: 使用大驼峰命名法 (PascalCase)，如 `Users`, `SystemRole`
- **函数/方法名**: 使用下划线命名法 (snake_case)，如 `get_user_info()`, `create_user()`
- **变量名**: 使用下划线命名法，如 `user_id`, `user_name`
- **常量**: 全大写下划线，如 `MAX_LENGTH`, `DEFAULT_STATUS`
- **模型字段**: 使用下划线命名法，添加verbose_name和help_text

### 注释规范
- **必须添加注释的地方**:
  - 类的说明文档（docstring）
  - 复杂业务逻辑方法的说明
  - 重要的业务逻辑块
  - 复杂的算法和数据处理
  - 模型字段的verbose_name和help_text
  - 常量和配置项的说明

- **注释格式**:
  ```python
  def process_user_data(self, user_data):
      """
      处理用户数据
      
      Args:
          user_data (dict): 用户数据字典
          
      Returns:
          dict: 处理后的用户数据
      """
      # 验证用户数据格式
      if not isinstance(user_data, dict):
          raise ValueError("用户数据必须是字典格式")
      
      # 处理用户头像路径
      avatar_url = self._process_avatar_url(user_data.get('avatar'))
      
      return processed_data
  ```

## Django 特定规范

### 模型 (Models) 规范
- 继承[CoreModel](mdc:dvadmin/utils/models.py)基类以获得软删除和审计功能
- 模型类名使用单数形式
- 字段定义要包含verbose_name和help_text
- 使用Meta类定义表名、排序等属性
- 表名使用table_prefix前缀

```python
class UserProfile(CoreModel):
    """用户档案模型"""
    user = models.OneToOneField(
        Users, 
        on_delete=models.CASCADE, 
        verbose_name="关联用户", 
        help_text="关联用户"
    )
    nickname = models.CharField(
        max_length=50, 
        verbose_name="昵称", 
        help_text="用户昵称"
    )
    
    class Meta:
        db_table = table_prefix + "user_profile"
        verbose_name = "用户档案"
        verbose_name_plural = verbose_name
```

### 视图 (Views) 规范
- 继承[CustomModelViewSet](mdc:dvadmin/utils/viewset.py)基类
- 使用类视图而非函数视图
- 视图方法要添加功能说明注释
- 错误处理要明确和用户友好

```python
class UserViewSet(CustomModelViewSet):
    """用户管理视图集"""
    queryset = Users.objects.all()
    serializer_class = UserSerializer
    
    def create(self, request, *args, **kwargs):
        """创建用户"""
        # 验证用户名是否已存在
        username = request.data.get('username')
        if Users.objects.filter(username=username).exists():
            return JsonResponse(code=400, msg="用户名已存在")
        
        return super().create(request, *args, **kwargs)
```

### 序列化器 (Serializers) 规范
- 继承ModelSerializer
- 显式定义fields字段
- 添加自定义验证方法
- 重写create和update方法处理复杂逻辑

### URL配置规范
- 使用命名空间组织URL
- URL名称使用下划线命名
- RESTful风格的URL设计

## 错误处理和日志

### 异常处理
- 使用try-except捕获可能的异常
- 记录详细的错误日志
- 返回用户友好的错误信息
- 使用自定义异常类

### 日志记录
- 使用Django的logging框架
- 记录关键业务操作
- 包含足够的上下文信息
- 敏感信息不记录到日志

```python
import logging

logger = logging.getLogger(__name__)

def process_payment(self, order_id, amount):
    """处理支付"""
    try:
        logger.info(f"开始处理订单{order_id}的支付，金额：{amount}")
        # 支付处理逻辑
        result = payment_service.charge(order_id, amount)
        logger.info(f"订单{order_id}支付成功")
        return result
    except PaymentException as e:
        logger.error(f"订单{order_id}支付失败：{str(e)}")
        raise
```

## 性能优化规范

### 数据库查询优化
- 使用select_related和prefetch_related避免N+1查询
- 合理使用数据库索引
- 避免在循环中进行数据库查询
- 使用Django ORM的批量操作

### 缓存策略
- 对于频繁查询的数据使用缓存
- 合理设置缓存过期时间
- 使用Redis等外部缓存系统

## 安全规范

### 数据验证
- 所有用户输入都要进行验证
- 使用Django的表单验证
- 防止SQL注入和XSS攻击
- 敏感操作要进行权限验证

### 权限控制
- 继承项目的权限基类
- 方法级别的权限控制
- 数据级别的权限过滤

## 测试规范

### 单元测试
- 重要的业务逻辑要编写测试用例
- 测试文件命名为test_*.py
- 测试方法命名以test_开头
- 保持测试的独立性和可重复性

## 代码提交规范

### 提交信息格式
```
[类型]: 简短描述

详细描述（可选）

相关Issue: #123
```

类型包括：
- feat: 新功能
- fix: 修复bug
- docs: 文档更新
- style: 代码格式调整
- refactor: 代码重构
- test: 测试相关
- chore: 构建过程或工具变动